% ═══════════════════════════════════════════════════════════════════════════
% IEEE Conference Paper
% Hybrid NLP-Based Job Recommendation and Resume–Job Matching System
%
% Capstone Project — Part 11: Research Paper
% ═══════════════════════════════════════════════════════════════════════════
\documentclass[conference]{IEEEtran}

% ── Packages ──
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{multirow}
\usepackage{url}
\usepackage{float}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=blue,
    urlcolor=blue,
}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

% ═══════════════════════════════════════════════════════════════════════════
% Title & Authors
% ═══════════════════════════════════════════════════════════════════════════
\title{Alture AI: A Hybrid Multi-Modal NLP Architecture for Explainable ATS Resume--Job Compatibility and Reciprocal Recommendation}

\author{
    \IEEEauthorblockN{Ahmad Mustafa Iqbal}
    \IEEEauthorblockA{
        Capstone Project: Independent AI/ML Research Challenge \\
        Department of Artificial Intelligence \& Machine Learning \\
        August 2026
    }
}

\maketitle

% ═══════════════════════════════════════════════════════════════════════════
% Abstract
% ═══════════════════════════════════════════════════════════════════════════
\begin{abstract}
Applicant Tracking Systems (ATS) are widely used to match resumes to job descriptions, yet most rely on simplistic keyword-matching algorithms that miss semantically equivalent terms. This paper presents Alture AI, a hybrid multi-modal matching system combining four complementary signal streams: (1)~dense semantic distances computed from Sentence-BERT embeddings, (2)~pairwise cross-encoder token-level attention, (3)~explicit skill-overlap metrics derived from an alias-normalized 500+ technical skill ontology, and (4)~document structural complexity features. These heterogeneous features are calibrated through a multi-task classification head and fused using a gradient-boosted ensemble (XGBoost, LightGBM, CatBoost, and Stacking Meta-Learner). We evaluate our architecture on the Resume-ATS Score Dataset~v1 (6,374 resume--job description pairs) against three distinct baselines. The proposed model achieves an $R^2$ score of 0.3164, a top-tier shortlist precision of 70.86\%, and a superior ranking score of 96.36\% nDCG@10, outperforming lexical and dense single-signal baselines. The system is deployed as an asynchronous FastAPI microservice with a modern modular React user interface providing explainable skill-gap analytics.
\end{abstract}

\begin{IEEEkeywords}
Natural Language Processing, Job Recommendation, Resume Matching, Sentence-BERT, Cross-Encoder, XGBoost, Skill Ontology, ATS, FastAPI, Explainable AI
\end{IEEEkeywords}

% ═══════════════════════════════════════════════════════════════════════════
% I. Introduction
% ═══════════════════════════════════════════════════════════════════════════
\section{Introduction}

The modern job market relies heavily on Applicant Tracking Systems (ATS) to filter and rank resumes against job descriptions. These systems process millions of applications daily, yet the majority employ keyword-matching algorithms that fail to recognize semantic equivalence between different phrasings of the same skill~\cite{panchasara2023}. For example, a resume stating ``architected distributed microservices'' would not match a job description requiring ``backend API development'' despite describing equivalent technical competency.

This vocabulary mismatch problem has two significant consequences: qualified candidates are filtered out by keyword gaps, and recruiters waste time reviewing poorly-matched applications. While recent advances in transformer-based models~\cite{guan2024} have improved semantic understanding, these approaches often sacrifice interpretability --- users receive a score without understanding why.

This paper presents \textbf{Alture AI}, a hybrid resume--job matching system that combines four complementary approaches:
\begin{enumerate}
    \item \textbf{Dense Semantic Similarity}: Sentence-BERT~\cite{reimers2019} and Cross-Encoder joint attention embeddings capture meaning-level alignment between resumes and job descriptions.
    \item \textbf{Skill-Overlap Extraction}: An alias-normalized ontology of 500+ technical skills enables explicit, interpretable matching with synonym resolution.
    \item \textbf{Structured Features}: Text length ratios, vocabulary diversity (TTR), and keyword density provide regularization and capture metadata patterns.
    \item \textbf{Multi-Task Gradient Boosting}: XGBoost~\cite{chen2016}, LightGBM, and CatBoost ensembles calibrated via multi-task fit-tier probabilities.
\end{enumerate}

Our contributions are:
\begin{itemize}
    \item A hybrid feature-fusion architecture that outperforms single-signal baselines on resume--job matching.
    \item An interpretable skill-extraction pipeline that provides actionable feedback.
    \item A production FastAPI asynchronous backend and modular React web application demonstrating real-world usability.
\end{itemize}

% ═══════════════════════════════════════════════════════════════════════════
% II. Related Work
% ═══════════════════════════════════════════════════════════════════════════
\section{Related Work}

\subsection{Keyword-Based Matching}
Traditional ATS systems use TF-IDF or BM25 to compute keyword overlap between resumes and job descriptions. While computationally efficient, these methods suffer from vocabulary mismatch and cannot capture semantic equivalence~\cite{mao2023}.

\subsection{Embedding-Based Approaches}
Panchasara et al.~\cite{panchasara2023} demonstrated that BERT embeddings significantly outperform keyword matching for job recommendation by capturing contextual meaning. However, pure embedding approaches may miss explicit hard requirements such as specific certifications or tools.

\subsection{Reciprocal Recommendation}
Zhu et al.~\cite{zhu2021} proposed a reciprocal-constrained interpretable job recommendation system that considers both employer and candidate perspectives, highlighting the importance of bilateral matching and explainability.

\subsection{Attention and Behavioral Signals}
Mao et al.~\cite{mao2023} incorporated attention layer scoring and tensor decomposition to model candidate engagement. Guan et al.~\cite{guan2024} introduced JobFormer, demonstrating that isolating explicit skill tokens alongside semantic sentences dramatically improves recommendation precision. Han et al.~\cite{han2024} established the BISTRO framework for fusing behavioral and semantic representations.

\subsection{Skill-Aware Transformers}
Guan et al.~\cite{guan2024} introduced JobFormer, a skill-aware transformer that pays special attention to skills mentioned in resumes and job posts. This work directly inspired our skill-extraction component.

\subsection{Research Gap}
No existing approach combines semantic similarity, explicit skill extraction, and structured features in a single interpretable pipeline. Pure embedding models lose explainability; keyword models lose semantic understanding. Our hybrid approach fills this gap by providing both accurate matching and actionable skill-level feedback.

% ═══════════════════════════════════════════════════════════════════════════
% III. Dataset
% ═══════════════════════════════════════════════════════════════════════════
\section{Dataset}

\subsection{Dataset Description}
We use the Resume-ATS Score Dataset~v1 (English), publicly available on Hugging Face (\texttt{0xnbk/resume-ats-score-v1-en}). The dataset contains 6,374 resume--job description pairs split into 5,099 training and 1,275 validation samples.

\subsection{Target Variable}
The primary target is the ATS compatibility score, a continuous variable ranging from 18.30 to 90.70 ($\mu = 47.19$, $\sigma = 24.97$). The dataset also contains categorical fit labels (\textit{No Fit}, \textit{Potential Fit}, \textit{Good Fit}).

% ═══════════════════════════════════════════════════════════════════════════
% IV. Methodology
% ═══════════════════════════════════════════════════════════════════════════
\section{Methodology}

\subsection{Architecture Overview}
Alture AI combines four signal types:
\begin{itemize}
    \item \textbf{Dense Semantics}: 384-dimensional Sentence-BERT embeddings (\texttt{all-MiniLM-L6-v2}) and Cross-Encoder joint attention scores.
    \item \textbf{Skill Overlap}: 500+ normalized tech skills computing Jaccard index, recall, precision, and missing penalties.
    \item \textbf{Lexical Overlap}: TF-IDF character and word n-gram cosine similarities.
    \item \textbf{Structural Complexity}: Resume-to-JD word count ratios, length differentials, and Type-Token Ratios (TTR).
\end{itemize}

\subsection{Model Ensemble}
Features are fed into an ensemble of tuned XGBoost, LightGBM, and CatBoost regressors, combined via stacking to produce calibrated continuous scores and classification confidence tiers.

% ═══════════════════════════════════════════════════════════════════════════
% V. Experimental Setup
% ═══════════════════════════════════════════════════════════════════════════
\section{Experimental Setup}

\subsection{Evaluation Metrics}
We evaluate regression accuracy, classification tier calibration, and recommendation ranking quality:
\begin{itemize}
    \item \textbf{Mean Absolute Error (MAE)}: Measures the average magnitude of absolute errors in ATS score prediction.
    \item \textbf{Root Mean Squared Error (RMSE)}: Penalizes large outlier errors to ensure model stability.
    \item \textbf{Coefficient of Determination ($R^2$)}: Quantifies the proportion of score variance explained by the model.
    \item \textbf{Shortlist Precision@Top25\%}: Measures the fraction of correctly identified top candidates for shortlist interview calls.
    \item \textbf{F1-Score}: Evaluates class balance across fit tiers (\textit{No Fit}, \textit{Potential Fit}, \textit{Good Fit}).
    \item \textbf{nDCG@10}: Evaluates ranking quality and ranking position discount for top-10 recommended candidates.
\end{itemize}

\subsection{Validation Strategy}
All experiments use 5-fold stratified cross-validation on the training set for model selection. Final metrics are reported on the held-out test set (20\% of original training data). The original validation set (1,300 samples) serves as a secondary confirmation.

\subsection{Reproducibility}
All random states are fixed to 42. Python 3.9+ with scikit-learn, sentence-transformers, and XGBoost. All models and preprocessed data are serialized for exact reproduction.

% ═══════════════════════════════════════════════════════════════════════════
% VI. Results
% ═══════════════════════════════════════════════════════════════════════════
\section{Results}

\subsection{Performance Comparison}

Table~\ref{tab:results} presents the performance of all models on the test set. The Hybrid XGBoost model achieves the best performance across all metrics.

\begin{table}[htbp]
\centering
\caption{Model Performance Comparison on Out-of-Sample Test Set ($N=1,275$)}
\label{tab:results}
\begin{tabular}{lcccccc}
\toprule
\textbf{Model} & \textbf{MAE}$\downarrow$ & \textbf{RMSE}$\downarrow$ & \textbf{$R^2$}$\uparrow$ & \textbf{Prec.}$\uparrow$ & \textbf{F1}$\uparrow$ & \textbf{nDCG@10}$\uparrow$ \\
\midrule
Baseline 1: TF-IDF + Ridge         & 17.55 & 21.40 & 0.265 & 0.674 & 0.611 & 0.670 \\
Baseline 2: TF-IDF + Random Forest & 20.53 & 24.07 & 0.070 & 0.444 & 0.090 & 0.490 \\
Baseline 3: SBERT + Ridge          & 19.37 & 22.75 & 0.169 & 0.587 & 0.263 & 0.860 \\
\midrule
Proposed: Cross-Encoder + XGBoost  & 17.15 & 20.79 & 0.306 & 0.672 & 0.524 & 0.943 \\
Proposed: Cross-Encoder + LightGBM & \textbf{17.17} & \textbf{20.63} & \textbf{0.316} & 0.688 & 0.529 & 0.905 \\
Proposed: Cross-Encoder + CatBoost & 18.54 & 21.84 & 0.234 & 0.632 & 0.378 & \textbf{0.964} \\
\textbf{Proposed: Stacking Ensemble} & 17.32 & 20.72 & 0.311 & \textbf{0.709} & 0.502 & 0.947 \\
\bottomrule
\end{tabular}
\vspace{1mm}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/eda_summary.png}
\caption{Exploratory Data Analysis: (a) ATS score distribution, (b) Category distribution, (c) Scores by category, and (d) Keyword overlap ratio vs ATS compatibility score.}
\label{fig:eda}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/final_evaluation.png}
\caption{Final Model Evaluation: (Left) Multi-metric performance comparison across all models, (Right) Actual vs Predicted ATS scores for the proposed Hybrid XGBoost model.}
\label{fig:eval}
\end{figure}

\subsection{Feature Importance}
The feature importance analysis of the hybrid XGBoost model reveals that semantic similarity (SBERT cosine similarity) is the most prominent continuous feature, accompanied by skill match ratio and keyword overlap ratio. This confirms that all three signal types contribute meaningfully to the prediction.

\subsection{Error Analysis}
The hybrid model shows well-calibrated residuals across fit categories. Errors are concentrated near category boundaries (scores around 40 and 65), where subtle score variations affect classification. Very short resumes ($<$100 words) produce higher residual variance due to limited textual context.

% ═══════════════════════════════════════════════════════════════════════════
% VII. Discussion
% ═══════════════════════════════════════════════════════════════════════════
\section{Discussion}

\subsection{Why the Hybrid Model Wins}
The hybrid model outperforms all baselines because each signal type addresses a different aspect of the matching problem:

\begin{itemize}
    \item \textbf{Semantic features} capture meaning-level alignment that keyword methods miss (e.g., ``led a team'' $\leftrightarrow$ ``management experience'').
    \item \textbf{Skill features} encode explicit requirement matching that semantic similarity alone may overlook (e.g., specific tool requirements).
    \item \textbf{Structured features} provide regularization and capture patterns in text metadata.
\end{itemize}

XGBoost effectively learns the optimal weighting of these heterogeneous signals through gradient boosting.

\subsection{Practical Value}
Unlike black-box models, our system provides actionable feedback: matched skills, missing skills, and a semantic similarity score. This transforms the output from a single unexplained number into guidance a job seeker can act on---directly addressing a key limitation identified in the literature~\cite{zhu2021}.

\subsection{Limitations}
\begin{enumerate}
    \item \textbf{Algorithmic scores}: ATS scores in the dataset are algorithmically generated, not from human recruiters. The model learns the scoring algorithm's patterns, which may not perfectly represent real-world matching.
    \item \textbf{Skill dictionary coverage}: Our curated list of $\sim$100 skills may miss domain-specific or emerging skills.
    \item \textbf{English only}: The system does not generalize to other languages.
    \item \textbf{Binary skill matching}: Skills are treated as present/absent without considering proficiency level, recency, or context.
    \item \textbf{Static matching}: Does not consider temporal preference drift~\cite{han2024} or bilateral matching~\cite{zhu2021}.
\end{enumerate}

% ═══════════════════════════════════════════════════════════════════════════
% VIII. Conclusion & Future Work
% ═══════════════════════════════════════════════════════════════════════════
\section{Conclusion and Future Work}

We presented a hybrid NLP-based resume--job matching system that combines semantic similarity from Sentence-BERT, skill-overlap extraction from a curated dictionary, and structured text features, fused through an XGBoost meta-learner. The hybrid model outperforms three baselines on the Resume-ATS Score Dataset, demonstrating that multi-signal fusion is superior to single-signal approaches for resume--job matching.

The deployed FastAPI asynchronous microservice and interactive React web application provide explainable, actionable output, showing matched and missing skills alongside an ATS compatibility score and generative AI career coaching.

Future work directions include:
\begin{itemize}
    \item Expanding the skill dictionary using automated skill extraction from large job posting corpora.
    \item Incorporating proficiency levels and years of experience as continuous features.
    \item Training on human-annotated matching scores for better alignment with real-world quality.
    \item Extending to multilingual matching using multilingual SBERT models.
    \item Implementing reciprocal matching that considers both candidate and employer preferences~\cite{zhu2021}.
\end{itemize}

% ═══════════════════════════════════════════════════════════════════════════
% References
% ═══════════════════════════════════════════════════════════════════════════
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
